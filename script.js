const toggle=document.querySelector('.menu-toggle');const nav=document.querySelector('.main-nav');toggle?.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',String(open));});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');toggle?.setAttribute('aria-expanded','false');}));document.getElementById('whatsapp-form')?.addEventListener('submit',(e)=>{e.preventDefault();const fd=new FormData(e.currentTarget);const name=fd.get('name')||'';const service=fd.get('service')||'';const date=fd.get('date')||'';const msg=`Hallo Silvia, ich bin ${name} und interessiere mich für ${service}. Mein Wunschtermin: ${date || 'noch offen'}. Haben Sie einen Termin frei?`;window.open(`https://wa.me/41763919556?text=${encodeURIComponent(msg)}`,'_blank','noopener');});for(const slider of document.querySelectorAll('.compare-slider')){const range=slider.querySelector('.compare-range');if(!range)continue;const update=()=>slider.style.setProperty('--position',`${range.value}%`);range.addEventListener('input',update);range.addEventListener('change',update);update();}